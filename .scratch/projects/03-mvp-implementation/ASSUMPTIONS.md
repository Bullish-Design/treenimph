# ASSUMPTIONS

- Guide text is authoritative when conflicts appear.
- Existing repository has no prior TreeNimph source files to preserve.
- Tree-sitter CLI is available via `devenv` for integration checks.
- Pushing requires configured git remote and credentials; if unavailable, local commits still proceed and push failure will be reported.
