// Generated by TreeNimph — do not edit manually.

module.exports = grammar({
  name: 'json',

  extras: $ => [
    /\\s+/,
  ],

  rules: {
    document: $ => $.value_node,

    value_node: $ => choice(
          $.json_object,
          $.array,
          $.string_rule,
          $.number_rule,
          $.true_lit,
          $.false_lit,
          $.null_lit,
        ),

    json_object: $ => seq(
          '{',
          optional(seq($.pair, repeat(seq(',', $.pair)), optional(','))),
          '}',
        ),

    pair: $ => seq(field('key', $.string_rule), ':', field('val', $.value_node)),

    array: $ => seq(
          '[',
          optional(seq($.value_node, repeat(seq(',', $.value_node)), optional(','))),
          ']',
        ),

    string_rule: $ => /[a-zA-Z_]+/,

    number_rule: $ => /-?[0-9]+(\\.[0-9]+)?([eE][+-]?[0-9]+)?/,

    true_lit: $ => 'true',

    false_lit: $ => 'false',

    null_lit: $ => 'null',
  }
});

